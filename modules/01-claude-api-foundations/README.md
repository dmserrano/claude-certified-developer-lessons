# Module 01 — Claude API foundations

> How to use: the home for module 01 — everything below builds toward its milestone · owned by /tutor (+ /editor for review). Status: ✅ built, awaiting `/editor`.

`type: technical` — Messages API, system prompts, params, streaming, token counting, model selection, prompt caching (TS SDK).

## What's here

- `tutorial.md` — the step-by-step build guide (four steps: Messages API → streaming → prompt caching → cost accounting).
- `guide.md` — concise review sheet; skim before interviews or the next module.
- `exercises/first-calls/` — the CLI you build in (fill the `TODO`s).
- `milestone/` — your deliverable + the acceptance check the /editor judges against.
- `review.md` — appears once /editor has critiqued the milestone.

## Milestone

A TS CLI that streams a response, caches a long system prompt, and prints token count + estimated cost per call. Acceptance check in [`milestone/README.md`](milestone/README.md).
