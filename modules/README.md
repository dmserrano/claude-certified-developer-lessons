# modules/

> Everything for each module lives in its own `NN-<name>/` directory (tutorial, guide, exercises, milestone, review) · owned by /tutor + /editor. One directory per module in `plan.md`; a directory appears once the Tutor starts that module.
